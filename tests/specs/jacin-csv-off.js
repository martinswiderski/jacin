require('./../../src/tool/test-signature')(__filename);

var csv = require('./../../src/csv');

describe('CSV Read from string', function () {
  it('without delimiter', function () {
      expect(false).toBe(true);
  });
  it('with a delimiter', function () {
      expect(false).toBe(true);
  });
});

describe('CSV Read from a file', function () {
  it('without delimiter', function () {
      expect(false).toBe(true);
  });
  it('with a delimiter', function () {
      expect(false).toBe(true);
  });
});

describe('Flatten array', function () {
  it('pre CSV stringify', function () {
      expect(false).toBe(true);
  });
});
